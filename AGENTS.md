# Agent notes

When you change or fix ktcalc, run the verification skill at `.cursor/skills/verify-ktcalc/` and attach the evidence (screenshots and the result transcript) to your PR. The skill starts the app, drives the real web UI, and writes evidence under `verify-artifacts/` (gitignored).
